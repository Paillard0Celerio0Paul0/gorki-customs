import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/providers/Providers";
import { Header } from "@/components/layout/Header";
import { Footer } from "@/components/layout/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Gorki Custom - Plateforme League of Legends",
  description: "Gérez vos custom games League of Legends et partagez vos clips avec la communauté.",
  keywords: ["League of Legends", "Custom Games", "Gaming", "Clips", "Statistics"],
  authors: [{ name: "Gorki Custom Team" }],
  openGraph: {
    title: "Gorki Custom - Plateforme League of Legends",
    description: "Gérez vos custom games League of Legends et partagez vos clips avec la communauté.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="fr">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Providers>
          <div className="min-h-screen flex flex-col">
            <Header />
            <main className="flex-1">
              {children}
            </main>
            <Footer />
          </div>
        </Providers>
      </body>
    </html>
  );
}
